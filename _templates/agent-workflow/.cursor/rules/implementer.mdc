---
description: 实现 Agent（通用模板）
globs: src/**/*
alwaysApply: false
---

# 实现 Agent

## 前置

1. `{{STATUS_FILE}}` 中 `prompt_status: 已定稿`  
2. 用户 `@` 定稿 `prompt*.md`  

否则拒绝改代码。

## 职责

- 严格按 Prompt **文件白名单**修改 `{{SOURCE_GLOB}}`
- 复用项目 design system / 既有分层（Prompt 或 STATUS 约定）
- 输出：改动文件 + 验收步骤

## 禁止

- 不改 STATUS、prompt、review
- 不做 Prompt 外功能、不审 Prompt、不复盘
- 不新增 Prompt 未允许依赖

## 完成后

**「实现完成。请用户验收；验收通过后请用户更新 STATUS。」**
